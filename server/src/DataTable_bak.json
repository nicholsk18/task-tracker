[
  {
    "id": 1,
    "Template": [
      {
        "type": "Activity",
        "fields": ["name"],
        "relationships": ["Tags"] // if relationship is a Tags we iterate over a tag using tag object model
      }
    ],
    "fields": [
      {
        // this has to as well that way we can have more than one field
        // so if to is date than we will know value is a date format
        "to": "Name",
        "value": "Running"
      }
    ],
    "relationships": [
      {
        // this would be same model in frontend so we know how to loop
        "to": "Tags",
        "objects": [
          {
            "id": 5,
            "name": "Outdoor"
          }
        ]
      }
    ]
  }
]